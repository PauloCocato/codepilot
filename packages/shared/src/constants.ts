/** Maximum number of retries for LLM calls and agent steps */
export const MAX_RETRIES = 3;

/** Default timeout for sandbox execution in milliseconds */
export const SANDBOX_TIMEOUT_MS = 60_000;

/** Default temperature for LLM completions */
export const DEFAULT_TEMPERATURE = 0.2;
